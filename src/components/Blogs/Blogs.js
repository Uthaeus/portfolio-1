
import classes from './Blogs.module.css';

const Blogs = () => {
    // when logged in be able to add a new blog

    return (
        <div className={classes.blogs}>
            <h1>Blogs Page</h1>
        </div>
    );
};


export default Blogs;